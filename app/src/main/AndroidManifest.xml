<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edu.fatec.glicocontrol">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <!-- Splash screen -->
        <activity
            android:name=".Splash"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity Principal -->
        <activity
            android:name=".UI.MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".UI.Cadastro" />
        <activity android:name=".UI.Controle" />
        <activity
            android:name=".UI.ListaControle"
            android:label="@string/title_activity_lista_controle"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UI.Imc" />
        <activity
            android:name=".UI.Alarme"
            android:label="@string/title_activity_alarme"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.ListaAlarme"
            android:label="@string/title_activity_lista_alarme"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest><!--
<manifest xmlns:android="http://schemas.android.com/apk/res/android" >

      <application>
           <activity android:name="com.edu.fatec.glicocontrol.Splash"
               android:label="@string/title_activity_splash"
               android:configChanges="orientation|keyboardHidden|screenSize"
               android:theme="@style/FullscreenTheme"
               >
           </activity>
       </application>

   </manifest>
-->
